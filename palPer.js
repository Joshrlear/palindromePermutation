// Get form
const myForm = document.getElementById(myForm);

// Get input text > loop through text > create object-
// storing any letters with frequency that is odd and-
// deleting any that are even > 'if' only one odd letter-
// left in object then return true, else false.